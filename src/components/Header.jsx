// src/components/Header.jsx
import React from 'react';
import './Header.css';

function Header() {
    return (
        <header className="header">
            <h1 className='h1'>Weather Dashboard</h1>
            <p>Just as the weather changes, so does life. Embrace the seasons and find beauty in every moment.</p>
        </header>
    );
}

export default Header;
